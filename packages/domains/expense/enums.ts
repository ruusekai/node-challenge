export enum ExpenseStatus{
  PENDING = 'pending',
  PROCESSED = 'processed'
}

export enum SortDirection{
  ASC = 'ASC',
  DESC = 'DESC',
}

export enum RangeAttribute{
  MAX = 'max',
  MIN = 'min',
}
